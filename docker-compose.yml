version: '3.8'

services:
  ew_suite:
    build: .
    container_name: cognitive_ew_suite
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
